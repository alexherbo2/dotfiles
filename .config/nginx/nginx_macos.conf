# nginx
# https://nginx.org

# Run nginx in the foreground.
daemon off;

user taupiqueur staff;

events {
}

http {
  server {
    location / {
      autoindex on;
      root /Users/taupiqueur/storage;
    }
  }
}
