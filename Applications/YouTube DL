#!/bin/sh

set -e

# install
# brew install gum yt-dlp

gum format "source directory:"
target_directory=$(gum file --file=false --directory)
cd "$target_directory"
while gum format "enter YouTube URL:"; gum input --placeholder="YouTube URL" | yt-dlp -a -
do
  :
done
gum format "goodbye"
