#!/bin/sh

# Convenience function to:
#
# – Print the command.
# – Run the command and die unless non-fatal.
#
# https://exherbo.org/docs/eapi/exheres-for-smarties.html#helper_functions

# Print the command.
#
# $ <command>
#
printf '$ %s\n' "$*"

# Run the command and die unless non-fatal.
"$@" || exit $?

# Add a final new line.
printf '\n'
