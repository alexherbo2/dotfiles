#!/bin/sh

nix-shell --packages postgresql_12 --run 'gem install pg'
nix-shell --packages ruby_2_7 sqlite postgresql_12 nodejs_latest yarn zlib libffi --run 'gem install rails'
nix-shell --packages sqlite --run 'gem install sqlite3'

gem install activerecord
gem install byebug
gem install faker
gem install pry
gem install pry-byebug
gem install rake
gem install rspec
gem install rubocop
gem install sinatra
gem install solargraph
