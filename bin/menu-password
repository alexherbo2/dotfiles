#!/usr/bin/env ruby

require 'yaml'

data = YAML.load(File.read(File.join(ENV['XDG_CONFIG_HOME'], 'passwords.yml')))

key = ARGV.join(' ')

if not key.empty?
  puts data[key].last
else
  puts data.keys.join("\n")
end
