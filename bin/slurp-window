#!/bin/sh

# https://swaywm.org
# https://wayland.emersion.fr/slurp/
# https://stedolan.github.io/jq/

jq_select_active_window='
  .. | select(.pid? and .visible?) |
  .rect | "\(.x),\(.y) \(.width)x\(.height)"
'

swaymsg -t get_tree | jq --raw-output "$jq_select_active_window" | slurp
