#!/bin/sh

wezterm start --class 'org.wezfurlong.wezterm · Floating' -- sh -c '
  setsid wl-copy --trim-newline < $(
    snippets get files |
    jq --raw-output .[] |
    fzf --preview-window=down:60% --preview "cat {}" --prompt="(s)>"
  )
'
