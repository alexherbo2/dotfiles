# Alacritty
# https://alacritty.org
# https://github.com/alacritty/alacritty/blob/master/alacritty.yml

import:

  # Color scheme – https://draculatheme.com/alacritty
  - ~/code~/dracula/alacritty/dracula.yml

# Appearance ───────────────────────────────────────────────────────────────────

# Add a bit of transparency to reduce eye strain.
window:
  opacity: 0.99

font:
  size: 16.0
  normal:
    family: PragmataPro Mono

font:
  normal:
    family: Ubuntu Mono

selection:
  save_to_clipboard: true

cursor:
  style:
    shape: Beam
    blinking: On

# Custom color palette (Tomorrow Night)
colors:
  primary:
    background: '#1d1f21'
    foreground: '#c5c8c6'
    bright_foreground: '#eaeaea'

# Keyboard ─────────────────────────────────────────────────────────────────────

# Keys:
# https://docs.rs/glutin/latest/glutin/event/enum.VirtualKeyCode.html
key_bindings:
  - { key: N, mods: Control|Shift, action: CreateNewWindow }
  - { key: N, mods: Command, action: CreateNewWindow }
