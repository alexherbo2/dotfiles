#!/bin/sh

alacritty_config=~/.config/alacritty/alacritty.yml

config=~/configuration/config/alacritty/alacritty.yml
keys=~/projects/alacritty-extended-keys/keys.yml
colors=~/repositories/github.com/dracula/alacritty/dracula.yml

cat "$config" "$keys" "$colors" > "$alacritty_config"
