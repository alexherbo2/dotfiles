#!/bin/sh

path=$(dirname "$0")

# Environment variables
XDG_CONFIG_HOME=${XDG_CONFIG_HOME:-~/.config}
CONFIG=$XDG_CONFIG_HOME/alacritty

# Write the configuration
mkdir -p "$CONFIG"
cat "$path/alacritty.yml" ~/repositories/github.com/dracula/alacritty/dracula.yml > "$CONFIG/alacritty.yml"
