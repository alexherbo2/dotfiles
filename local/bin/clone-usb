#!/bin/sh

sudo clone -null ~/root ~/Disk/USB
status=$?
if test -n "$DISPLAY"; then
  if test "$status" = 0; then
    notify-send 'USB::Clone' "Status: $status"
  else
    notify-send --urgency critical 'USB::Clone' "Status: $status"
  fi
fi
